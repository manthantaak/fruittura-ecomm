import { LoginForm } from "@/components/auth/login-form"

export const metadata = {
  title: "Login | Fruittura",
  description: "Sign in to your Fruittura account",
}

export default function LoginPage() {
  return <LoginForm />
}
